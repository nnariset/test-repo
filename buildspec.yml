version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - pip install -r requirements.txt
  build:
    commands:
      - echo Building the Python app...
      - python app.py
artifacts:
  files:
    - '**/*'
